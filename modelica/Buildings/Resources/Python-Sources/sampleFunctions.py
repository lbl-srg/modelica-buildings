# Make sure that the python path is set, such as by running
# export PYTHONPATH=`pwd`
def multiply(doubles):
    ''' Multiply the arguments by two and return the result.
    '''
    returnDoubles = []
    for d in doubles:
        returnDoubles.append(2. * d)
    return returnDoubles

